# changing date in saved invoice does not update date in sale/purchase_transaction table
# TODO: find profit
    # cost column in si_detail
    # transport column in product
    # rate from vendor_product + transport = cost column in product
    # auto-retrieve cost in si_detail
    # color when cost column is empty
    # display summary showing profit per invoice
# fix owner_product table rates for barrel nipple, extension
# owner_product incorrect rate display shanti const. stop cock society
# how to change discount while creating invoice?
# prompt_toolkit shortcut for tab+enter
# invoice class design change to avoid properties order relevancy
# PriceList items are probably saving to owner_product rates with null rates. They, therefore appear in '.<product_name>' in 'sale_invoice command' prompt.
